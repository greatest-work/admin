"use strict";
var vue = require("vue");
var Tbody = vue.defineComponent({
  name: "Tbody",
  setup(_, {
    slots
  }) {
    return () => {
      var _a, _b, _c;
      return vue.createVNode((_b = (_a = slots.tbody) == null ? void 0 : _a.call(slots)[0]) != null ? _b : "tbody", null, (_c = slots.default) == null ? void 0 : _c.call(slots));
    };
  }
});
module.exports = Tbody;
