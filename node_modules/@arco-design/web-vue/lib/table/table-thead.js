"use strict";
var vue = require("vue");
var Thead = vue.defineComponent({
  name: "Thead",
  setup(_, {
    slots
  }) {
    return () => {
      var _a, _b, _c;
      return vue.createVNode((_b = (_a = slots.thead) == null ? void 0 : _a.call(slots)[0]) != null ? _b : "thead", null, (_c = slots.default) == null ? void 0 : _c.call(slots));
    };
  }
});
module.exports = Thead;
