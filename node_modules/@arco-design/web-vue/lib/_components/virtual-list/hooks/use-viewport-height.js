"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports[Symbol.toStringTag] = "Module";
var vue = require("vue");
var is = require("../../../_utils/is.js");
const DEFAULT_HEIGHT = 200;
function useViewportHeight(props) {
  const { height } = vue.toRefs(props);
  const viewportHeight = vue.ref(DEFAULT_HEIGHT);
  const setViewportHeight = (val) => {
    viewportHeight.value = val;
  };
  vue.watchEffect(() => {
    if (is.isNumber(height.value)) {
      viewportHeight.value = height.value;
    }
  });
  return {
    viewportHeight,
    setViewportHeight,
    needMeasureViewportHeight: vue.computed(() => !is.isNumber(height.value))
  };
}
exports.useViewportHeight = useViewportHeight;
